#include "seahorn/seahorn.h"

int main(int argc, char * argv[]) {
  int i, j, k, n;

  k = unknown ();
  n = unknown ();
  
  if (k >= n && n >= 1) {


  int i = unknown1 ();

  j = 0;
  while (j <= n - 1) {
    j++;
    k--;
  }

  if (j >= n) {
    sassert(k > -1);
  }
  }

  return 0;
}
