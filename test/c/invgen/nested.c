 #include "seahorn/seahorn.h"


void main() {
  int i,k,n,l;

  //tmpl("(le(n,i,k),le(n,i,k))");

  for (k=1;k<n;k++){
    //    assert(k<=n);
    //   assert(1<=k);
    for (i=1;i<n;i++) {
      //assert(1<=k);
      // assert(1<=i);
      // assert(i<=n);
    }
  }
  sassert(1<=k);
 }
